spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://localhost:3306/projecteidiko2
spring.datasource.username=root
spring.datasource.password=root

server.port=2627



#create table users(username varchar(50) not null primary key,password varchar(500) not null,enabled boolean not null);
#create table authorities(username varchar(50) not null,authority varchar950) not null,constraint fk_authorities_usersinfo foreign key(username)
#references usersinfo(username)